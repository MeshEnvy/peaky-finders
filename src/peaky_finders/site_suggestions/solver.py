"""Solver step budget constants for ``peaky build --suggest``."""

from __future__ import annotations

# ``--suggest`` with no N: run until strategy ``planning_complete`` (no step cap).
SOLVE_UNTIL_COMPLETE = -1
